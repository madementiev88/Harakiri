services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: harakiri
      POSTGRES_USER: harakiri
      POSTGRES_PASSWORD: harakiri
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
